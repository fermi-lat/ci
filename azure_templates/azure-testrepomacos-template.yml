parameters:
  repo: ''

jobs:
- job: fermitoolstestbuild
  displayName: 'Run $(repo) tests on MacOS'
  timeoutInMinutes: 150
  pool:
    vmImage: 'macOS-latest'
  
  steps:
  - bash: |
      echo 'Placeholder for running $(repo) tests on MacOS'
    displayName: 'Test Repo'
